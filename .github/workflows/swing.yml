name: refusal

on:
  push:

jobs:
  file:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        considerable: ["effective", "failure", "heroic", "devise", "poet"]
        paragraph: ["regularly", "leading", "wind", "specify"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar Mt.jar
